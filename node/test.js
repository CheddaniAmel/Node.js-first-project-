/* Global object */
console.log(global.setTimeout(() => {
    console.log("global is a bit like the window object when we work with js inside the browser which is its global object and in node the global object in global  ")
    
}, 1000));

// Modules & require
/*i created 2 files modules and people
we want to import the info inside the people file and use them in the module file 
 */


